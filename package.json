{
  "name": "pr-status-notifier",
  "version": "1.0.0",
  "description": "GitHub Action to notify when PR checks pass",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "jest",
    "lint": "eslint .",
    "prepare": "husky install"
  },
  "keywords": [
    "github",
    "actions",
    "pr",
    "checks"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.56.0",
    "husky": "^9.0.6",
    "jest": "^29.7.0"
  }
}